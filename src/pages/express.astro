---
// Buy-now remains a safe GET history entry, but Tiểu Viện uses its own local
// checkout instead of creating a provider session or inventory hold here.
const query = Astro.url.searchParams.toString();
return Astro.redirect(`/checkout${query ? `?${query}` : ''}`, 302);
---
